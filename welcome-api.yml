server:
  port: 8081


msg: Testing welcome-api Config-Server in Microservices Architecture with Spring Boot 3.

spring:
  boot:
    admin:
      client:
        url: http://localhost:1111/
        
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
      
management:
  endpoints:
    web:
      exposure:
        include: '*'
