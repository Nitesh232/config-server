server:
  port: 8091

msg: Testing Greet API with config Server in Microservices-Architecture.


spring:   
  boot:
    admin:
      client:
        url: http://localhost:1111/
        
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
      
management:
  endpoints:
    web:
      exposure:
        include: '*'
